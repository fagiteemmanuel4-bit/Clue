'use client'

import { useEffect, useMemo, useRef, useState } from 'react'
import { Archive, Copy, FileText, Folder, Menu, Mic, Paperclip, Plus, RotateCcw, Search, Send, Settings, Sparkles, Square, X, Globe, Code2, Image as ImageIcon, LogOut } from 'lucide-react'
import Link from 'next/link'

type Role='user'|'assistant'
type Message={id:string;role:Role;content:string;attachment?:{name:string;type:string}}
type Conversation={id:string;title:string;messages:Message[];archived?:boolean;updatedAt:number}
type User={id:string;email:string;displayName:string|null}
const tools=[{label:'Web',icon:Globe},{label:'Code',icon:Code2},{label:'Image',icon:ImageIcon}]
const guestKey='clue.guest.v1'; const guestLimit=5
function makeConversation():Conversation{return{id:crypto.randomUUID(),title:'New conversation',messages:[],updatedAt:Date.now()}}

export default function Home(){
 const [open,setOpen]=useState(false),[user,setUser]=useState<User|null>(null),[conversations,setConversations]=useState<Conversation[]>([]),[activeId,setActiveId]=useState('')
 const [message,setMessage]=useState(''),[busy,setBusy]=useState(false),[toolsOpen,setToolsOpen]=useState(false),[activeTool,setActiveTool]=useState(''),[search,setSearch]=useState(''),[attachment,setAttachment]=useState<{name:string;type:string}|undefined>(),[listening,setListening]=useState(false),[guestCount,setGuestCount]=useState(0),[showContinue,setShowContinue]=useState(false)
 const mediaRef=useRef<MediaRecorder|null>(null),chunksRef=useRef<Blob[]>([]),abortRef=useRef<AbortController|null>(null),messagesRef=useRef<HTMLDivElement>(null)
 useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(d=>{if(d.user)setUser(d.user)}).catch(()=>{});try{setGuestCount(Number(localStorage.getItem(guestKey)||0))}catch{}},[])
 useEffect(()=>{if(user){fetch('/api/conversations').then(r=>r.ok?r.json():null).then(d=>{if(d?.conversations)setConversations(d.conversations.map((c:Record<string,unknown>)=>({...c,messages:[],updatedAt:new Date(String(c.updatedAt)).getTime()})))}).catch(()=>{})}else{try{const saved=JSON.parse(localStorage.getItem('clue.conversations.v1')||'[]');if(Array.isArray(saved))setConversations(saved)}catch{}}},[user])
 useEffect(()=>{if(!user)localStorage.setItem('clue.conversations.v1',JSON.stringify(conversations))},[conversations,user])
 useEffect(()=>{messagesRef.current?.scrollTo({top:messagesRef.current.scrollHeight,behavior:'smooth'})},[activeId,conversations])
 const active=conversations.find(c=>c.id===activeId),messages=active?.messages||[]
 const greeting=useMemo(()=>{const h=new Date().getHours();if(h<5)return['Still up,','night owl.'];if(h<12)return['Good','morning.'];if(h<17)return['Good','afternoon.'];if(h<21)return['Good','evening.'];return['Winding down','for the night?']},[])
 const visible=conversations.filter(c=>!c.archived&&c.title.toLowerCase().includes(search.toLowerCase()))
 function updateConversation(id:string,patch:Partial<Conversation>){setConversations(p=>p.map(c=>c.id===id?{...c,...patch,updatedAt:Date.now()}:c))}
 async function startNew(){setMessage('');setAttachment(undefined);setOpen(false);if(user){try{const r=await fetch('/api/conversations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})});const d=await r.json();if(r.ok&&d.conversation){const c={...d.conversation,messages:[],updatedAt:Date.now()};setConversations(p=>[c,...p]);setActiveId(c.id);return}}catch{}}const c=makeConversation();setConversations(p=>[c,...p]);setActiveId(c.id)}
 async function submit(){const text=message.trim();if(!text||busy)return;if(!user&&guestCount>=guestLimit){setShowContinue(true);return}
  let convo=active;if(!convo){await startNew();return}
  const userMsg={id:crypto.randomUUID(),role:'user' as const,content:text,attachment};const next=[...convo.messages,userMsg];const title=convo.messages.length?convo.title:text.slice(0,48);updateConversation(convo.id,{messages:next,title});setMessage('');setAttachment(undefined);setToolsOpen(false);setBusy(true);abortRef.current=new AbortController();const assistantId=crypto.randomUUID();updateConversation(convo.id,{messages:[...next,{id:assistantId,role:'assistant',content:''}]})
  if(!user){const n=guestCount+1;setGuestCount(n);localStorage.setItem(guestKey,String(n));if(n>=guestLimit)setShowContinue(true)}
  try{const response=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:next.map(({role,content})=>({role,content})),model:activeTool==='Code'?'free':undefined}),signal:abortRef.current.signal});if(!response.ok||!response.body)throw new Error('request failed');const reader=response.body.getReader(),decoder=new TextDecoder();let full='';while(true){const {done,value}=await reader.read();if(done)break;full+=decoder.decode(value,{stream:true});updateConversation(convo.id,{messages:[...next,{id:assistantId,role:'assistant',content:full}]})}if(!full)throw new Error('empty response')
   if(user){await fetch(`/api/conversations/${convo.id}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({role:'user',contentText:text,contentType:'text'})});await fetch(`/api/conversations/${convo.id}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({role:'assistant',contentText:full,contentType:'text'})})}
  }catch(e){if(e instanceof DOMException&&e.name==='AbortError')return;updateConversation(convo.id,{messages:[...next,{id:assistantId,role:'assistant',content:'Clue could not connect right now. Try again.'}]})}finally{setBusy(false);abortRef.current=null}}
 function cancel(){abortRef.current?.abort();setBusy(false)}
 function regenerate(index:number){const u=messages.slice(0,index).reverse().find(m=>m.role==='user');if(!u||busy)return;updateConversation(activeId,{messages:messages.slice(0,index)});setMessage(u.content)}
 function editMessage(index:number){if(messages[index]?.role!=='user'||busy)return;setMessage(messages[index].content);updateConversation(activeId,{messages:messages.slice(0,index)})}
 async function copy(text:string){await navigator.clipboard?.writeText(text)}
 async function logout(){await fetch('/api/auth/logout',{method:'POST'});location.reload()}
 async function toggleVoice(){if(listening&&mediaRef.current){mediaRef.current.stop();setListening(false);return}if(!navigator.mediaDevices?.getUserMedia)return;try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});const recorder=new MediaRecorder(stream);chunksRef.current=[];recorder.ondataavailable=e=>chunksRef.current.push(e.data);recorder.onstop=()=>{stream.getTracks().forEach(t=>t.stop());setMessage('Voice note recorded — review before sending.')};mediaRef.current=recorder;recorder.start();setListening(true)}catch{setListening(false)}}
 function onFile(e:React.ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(file)setAttachment({name:file.name,type:file.type||'file'});e.currentTarget.value=''}
 return <main className="app"><div className="glow"/><div className={`overlay ${open?'visible':''}`} onClick={()=>setOpen(false)}/><aside className={`sidebar ${open?'open':''}`} aria-hidden={!open}><div className="brand"><span>Clue</span></div><div className="side-inner"><nav><button className="nav primary" onClick={startNew}><Plus/> New chat</button><button className="nav"><Folder/> Projects</button><button className="nav"><FileText/> Files</button><button className="nav" onClick={()=>activeId&&setConversations(p=>p.map(c=>c.id===activeId?{...c,archived:true}:c))}><Archive/> Archive</button><div className="search-box"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search chats" aria-label="Search conversations"/></div></nav><div className="history">{visible.map(c=><button className={`history-item ${c.id===activeId?'active':''}`} key={c.id} onClick={()=>{setActiveId(c.id);setOpen(false)}}>{c.title}</button>)}</div></div><footer>{user?<><div className="plan"><span>{user.displayName||user.email}</span></div><button className="nav" onClick={logout}><LogOut/> Sign out</button></>:<Link href="/login" className="upgrade">Sign in to Clue</Link>}<button className="nav"><Settings/> Settings</button></footer></aside><header>{(user||messages.length>0)&&<button className="circle menu" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X/>:<Menu/>}</button>}{!user&&messages.length===0?<Link className="login-link" href="/login">Log in</Link>:user&&<span className="user-pill">{user.displayName||user.email.split('@')[0]}</span>}</header><section className={`conversation ${messages.length?'has-messages':''}`} ref={messagesRef}>{messages.length===0?<div className="hero"><h1><span>{greeting[0]} </span><em>{greeting[1]}</em></h1><p>Ask anything, or start with what&apos;s already on your mind.</p></div>:<div className="messages">{messages.map((m,i)=><article className={`message ${m.role}`} key={m.id}><div className="bubble">{m.attachment&&<small className="attachment"><Paperclip/> {m.attachment.name}</small>}<div className="content">{m.content||(busy?' ':'No response.')}</div>{m.role==='assistant'&&m.content&&<div className="message-actions"><button onClick={()=>copy(m.content)} aria-label="Copy"><Copy/></button><button onClick={()=>regenerate(i)} aria-label="Regenerate"><RotateCcw/></button></div>}{m.role==='user'&&<button className="edit" onClick={()=>editMessage(i)}>Edit</button>}</div></article>)}{busy&&<div className="typing"><span/><span/><span/></div>}</div>}</section><section className="composer-wrap">{toolsOpen&&<div className="tool-menu" role="menu">{tools.map(({label,icon:Icon})=><button key={label} onClick={()=>{setActiveTool(label);setToolsOpen(false)}}><Icon/>{label}</button>)}<label><Paperclip/> Attach<input hidden type="file" accept="image/*,.pdf,.txt,.csv,.doc,.docx,.xls,.xlsx,.json,audio/*,video/*" onChange={onFile}/></label></div>}{attachment&&<div className="attachment-pill"><Paperclip/> {attachment.name}<button onClick={()=>setAttachment(undefined)} aria-label="Remove attachment"><X/></button></div>}<div className="composer"><button className={`circle tool ${toolsOpen?'selected':''}`} onClick={()=>setToolsOpen(!toolsOpen)} aria-label="Attachments and tools"><Plus/></button><textarea value={message} onChange={e=>setMessage(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}}} placeholder={activeTool?`Ask Clue with ${activeTool}...`:'Message...'} rows={1}/><button className={`circle mic ${listening?'listening':''}`} onClick={toggleVoice} aria-label="Voice note"><Mic/></button>{busy?<button className="circle send ready" onClick={cancel} aria-label="Stop"><Square/></button>:<button className={`circle send ${message.trim()?'ready':''}`} onClick={submit} aria-label="Send"><Send/></button>}</div><p className="hint">{!user?`${Math.max(0,guestLimit-guestCount)} guest messages left · `:''}Private by default.</p></section>{showContinue&&<div className="conversion"><div className="conversion-sheet"><button className="sheet-close" onClick={()=>setShowContinue(false)} aria-label="Close"><X/></button><h2>Continue with Clue</h2><p>Keep your conversation by signing in or creating an account.</p><Link href="/login" className="auth-primary">Continue with email</Link><button className="auth-provider" disabled>Continue with Google</button><button className="auth-provider" disabled>Continue with Apple</button></div></div>}</main>
}
