'use client'

import { FormEvent, useState } from 'react'
import Link from 'next/link'
import { useRouter } from 'next/navigation'

export default function LoginPage() {
  const router = useRouter()
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)

  async function submit(e: FormEvent) {
    e.preventDefault(); setError(''); setBusy(true)
    try {
      const res = await fetch('/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) })
      const data = await res.json()
      if (!res.ok) throw new Error(data.error || 'Unable to sign in')
      router.push('/onboarding'); router.refresh()
    } catch (err) { setError(err instanceof Error ? err.message : 'Unable to sign in') } finally { setBusy(false) }
  }

  return <main className="auth-screen"><div className="auth-glow"/><section className="auth-card"><Link href="/" className="auth-logo">Clue<span>.</span></Link><div className="auth-heading"><p className="auth-eyebrow">WELCOME BACK</p><h1>Log in to Clue</h1><p>Continue your conversations and pick up right where you left off.</p></div><form onSubmit={submit} className="auth-form"><label><span>Email</span><input required type="email" autoComplete="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="you@example.com" /></label><label><span>Password</span><input required type="password" autoComplete="current-password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Your password" /></label>{error && <div className="auth-error" role="alert">{error}</div>}<button className="auth-submit" disabled={busy}>{busy ? 'Signing in…' : 'Continue'}</button></form><div className="auth-divider"><span>or</span></div><button className="auth-provider" disabled><span className="provider-dot">G</span>Continue with Google<span/></button><button className="auth-provider" disabled><span className="provider-dot apple">●</span>Continue with Apple<span/></button><p className="auth-provider-note">Social sign-in will be available when the providers are connected.</p><div className="auth-footer"><span>New to Clue?</span><Link href="/signup">Create an account</Link></div><Link href="/" className="auth-back">← Back to Clue</Link></section><style jsx>{` .auth-screen{min-height:100dvh;background:#f6f5f1;color:#17171a;display:grid;place-items:center;padding:24px;position:relative;overflow:hidden;font-family:Inter,system-ui,sans-serif}.auth-glow{position:absolute;width:520px;height:520px;border-radius:50%;background:radial-gradient(circle,rgba(23,23,26,.07),transparent 68%);filter:blur(12px);pointer-events:none}.auth-card{position:relative;width:min(430px,100%);padding:34px;border:1px solid rgba(20,20,25,.07);border-radius:28px;background:rgba(255,255,255,.88);backdrop-filter:blur(22px);box-shadow:0 28px 90px rgba(20,20,25,.12)}.auth-logo{display:inline-block;color:#17171a;text-decoration:none;font-family:Fraunces,Georgia,serif;font-size:27px;font-style:italic;letter-spacing:-.04em}.auth-logo span{font-style:normal}.auth-heading{margin:42px 0 26px}.auth-eyebrow{margin:0 0 8px;color:#a0a0a7;font-size:10px;font-weight:700;letter-spacing:.14em}.auth-heading h1{margin:0;font-family:Fraunces,Georgia,serif;font-size:40px;font-weight:400;letter-spacing:-.035em}.auth-heading>p:last-child{margin:10px 0 0;color:#707078;font-size:13.5px;line-height:1.6}.auth-form{display:grid;gap:14px}.auth-form label{display:grid;gap:7px}.auth-form label span{font-size:12px;font-weight:600}.auth-form input{width:100%;height:48px;border:1px solid rgba(20,20,25,.1);border-radius:13px;background:#fbfaf7;color:#17171a;outline:0;padding:0 14px;font-size:14px;transition:.18s}.auth-form input::placeholder{color:#b0b0b6}.auth-form input:focus{border-color:rgba(20,20,25,.28);background:#fff;box-shadow:0 0 0 4px rgba(20,20,25,.045)}.auth-submit{height:48px;border:0;border-radius:13px;background:#17171a;color:#fff;font-size:13px;font-weight:650;cursor:pointer;margin-top:3px;transition:.18s}.auth-submit:hover{transform:translateY(-1px);box-shadow:0 10px 25px rgba(20,20,25,.15)}.auth-submit:disabled{opacity:.6;cursor:wait;transform:none}.auth-error{padding:10px 12px;border:1px solid rgba(180,35,24,.16);background:#fff6f5;color:#a42318;border-radius:11px;font-size:12px;line-height:1.45}.auth-divider{display:flex;align-items:center;gap:10px;margin:20px 0 10px;color:#b0b0b5;font-size:11px}.auth-divider:before,.auth-divider:after{content:'';height:1px;background:rgba(20,20,25,.07);flex:1}.auth-provider{width:100%;height:45px;margin-top:7px;border:1px solid rgba(20,20,25,.08);border-radius:12px;background:#fff;color:#77777f;display:flex;align-items:center;justify-content:space-between;padding:0 13px;font-size:12.5px}.auth-provider:disabled{opacity:.7}.provider-dot{width:22px;height:22px;display:grid;place-items:center;border-radius:50%;background:#f0efeb;color:#555;font-size:11px;font-weight:700}.provider-dot.apple{font-size:8px}.auth-provider-note{margin:10px 0 0;text-align:center;color:#aaaab0;font-size:10.5px;line-height:1.45}.auth-footer{display:flex;justify-content:center;gap:5px;margin-top:22px;color:#77777f;font-size:12px}.auth-footer a{color:#17171a;font-weight:650;text-decoration:none}.auth-back{display:block;text-align:center;margin-top:18px;color:#a0a0a7;text-decoration:none;font-size:11px}.auth-back:hover{color:#17171a}@media(max-width:520px){.auth-screen{padding:14px;align-items:start;padding-top:8vh}.auth-card{padding:25px 20px;border-radius:23px}.auth-heading{margin-top:34px}.auth-heading h1{font-size:34px}}`}</style></main>
}
